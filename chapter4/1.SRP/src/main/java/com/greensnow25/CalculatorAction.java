package com.greensnow25;

/**
 * public class CalculatorAction.
 *
 * @author greensnow25.
 * @version 1.
 * @since 20.03.2017.
 */
public interface CalculatorAction {
    /**
     * makeAction with numbers.
     * @param one number.
     * @param two number.
     * @return result.
     */
    double makeAction(double one, double two);
}
